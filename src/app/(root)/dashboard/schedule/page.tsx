import HeroSchedule from "./hero-schedule";
import ClassTabs from "./schedule";

const Page = async () => {
  return (
    <main className=" ">
      <HeroSchedule />
      <ClassTabs />
    </main>
  );
};

export default Page;
